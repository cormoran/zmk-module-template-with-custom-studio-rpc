name: Pre-commit Checks

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci
        working-directory: web

      - name: Generate types from proto
        run: npm run generate
        working-directory: web

      - name: Install packages for ZMK tests
        run: |
          sudo apt update
          sudo apt install -y --no-install-recommends git cmake ninja-build gperf \
              ccache dfu-util device-tree-compiler wget \
              python3-dev python3-pip python3-setuptools python3-tk python3-wheel xz-utils file \
              make gcc gcc-multilib g++-multilib libsdl2-dev libmagic1

      - name: Install West
        run: pip3 install --user -U west pyelftools grpcio-tools

      - name: Cache Zephyr SDK
        id: cache-zephyr
        uses: actions/cache@v4
        with:
          path: /home/runner/zephyr-sdk-0.16.3
          key: zephyr-sdk-0-16-3-arm-zephyr-eabi

      - name: Download Zephyr SDK
        if: ${{ steps.cache-zephyr.outputs.cache-hit != 'true' }}
        run: |
          cd ~
          wget https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v0.16.3/zephyr-sdk-0.16.3_linux-x86_64.tar.xz
          wget -O - https://github.com/zephyrproject-rtos/sdk-ng/releases/download/v0.16.3/sha256.sum | shasum --check --ignore-missing
          tar xvf zephyr-sdk-0.16.3_linux-x86_64.tar.xz

      - name: Install Zephyr SDK components
        run: |
          cd ~/zephyr-sdk-0.16.3
          ./setup.sh -h -c -t arm-zephyr-eabi

      - name: Cache west modules
        uses: actions/cache@v4
        continue-on-error: true
        env:
          cache_name: cache-west-modules
        with:
          path: dependencies/
          key: ${{ runner.os }}-build-${{ env.cache_name }}-${{hashFiles('west/**/west*.yml') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache_name }}-

      - name: Setup ZMK environment
        run: |
          west init -l west --mf west-test-standalone.yml
          west update --narrow
          west zephyr-export

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1
