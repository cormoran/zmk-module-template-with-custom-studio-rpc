# The split central additionally enables ZMK Studio + this module's custom RPC
# subsystem: the sample RPC is what triggers the relay to the peripheral.
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_TEMPLATE_FEATURE_STUDIO_RPC=y
