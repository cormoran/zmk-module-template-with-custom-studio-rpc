# List of this module's dependencies for testing
manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: cormoran
      url-base: https://github.com/cormoran
  projects:
    # TODO: switch revision back to main+custom-studio-protocol once
    # cormoran/zmk's fix/local-id-map-writable-section branch merges. The BLE
    # (BabbleSim) tests need its two fixes (writable behavior local-id map
    # section + settings_subsys_init before dynamic handler registration);
    # without them the split central segfaults / never starts BLE on
    # nrf52_bsim.
    - name: zmk
      remote: cormoran
      revision: fffa339cf6f5c45366ab332d2b512f1c3c300753
      import:
        file: app/west.yml
      clone-depth: 1
    # Provides the `west zmk-renode-test` / `west zmk-ble-test` commands, the
    # shared `ble-studio-host` app + `studio_requests.json` DSL, and (as a
    # Zephyr module -- see its own zephyr/module.yml) the Renode-only Studio
    # RPC UART transport + the renode-studio-uart snippet used by
    # tests/zmk-config/build.yaml's renode_smoke_test artifact.
    # TODO: switch revision back to main once
    # cormoran/zmk-west-commands#18/#19 merge.
    - name: zmk-west-commands
      remote: cormoran
      revision: fd49434abfcb22e48a50c15be1c78ab57b37dc50
      import: true
